<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QMC</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/styles-tablet.css">
    <link rel="stylesheet" href="/css/styles-desktop.css">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Shade&display=swap" rel="stylesheet">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Work+Sans&display=swap" rel="stylesheet">

    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />

    <script src="/js/validateProdCr.js"></script>

</head>

<body>

    <header>

        <%- include("./parcials/header") %>


            <%- include("./parcials/nav") %>

    </header>

    <main class="main-form">
        <div class="errorsCreateForm" style="color:orangered">
            <ul></ul>
        </div>
        <form action="/products/createProd" method="post" enctype="multipart/form-data" class="cont-form createProd">
            <div class="cont-create">
                <div class="titulo-form">
                    <h1>Crear Producto</h1>
                </div>
                <div class="wrap-form-create name">
                    <label for="name">Nombre del producto</label>
                    <input type="text" name="name" class="name">
                    <% if (locals.errors && errors.name) { %>
                        <div class="text-danger">
                            <%= errors.name.msg %>
                        </div>
                        <% } %>
                </div>
                <div class="wrap-form-create desc">
                    <label for="description">Descripcion</label>
                    <input type="text" name="desc" Class="desc">
                    <% if (locals.errors && errors.description) { %>
                        <div class="text-danger">
                            <%= errors.description.msg %>
                        </div>
                        <% } %>
                </div>
                <div class="wrap-form-create size">
                    <label for="size">Tamaño</label>
                    <input type="text" name="size" class="size">
                    <% if (locals.errors && errors.size) { %>
                        <div class="text-danger">
                            <%= errors.size.msg %>
                        </div>
                        <% } %>
                </div>

                <div class="wrap-form-create price">
                    <label for="price">Precio</label>
                    <input type="text" name="price" class="price">
                    <% if (locals.errors && errors.price) { %>
                        <div class="text-danger">
                            <%= errors.price.msg %>
                        </div>
                        <% } %>
                </div>
                <div class="wrap-form-create disc">
                    <label for="disc">Descuento</label>
                    <input type="number" name="disc" class="disc">
                    <% if (locals.errors && errors.disc) { %>
                        <div class="text-danger">
                            <%= errors.disc.msg %>
                        </div>
                        <% } %>
                </div>

                <div class="separador"></div>
                <div class="wrap-form category">
                    <label for="idCategory"><b>Categoria</b></label>
                    <select name="idCategory" id="idCategory" class="category">
                                    <option value="">Elegí</option>
                                    <option value="gaming" >Gaming</option>
                                    <option value="desktop" >Desktop</option>
                        <option value="office" >Office</option>
                    </select>
                    <% if (locals.errors && errors.idCategory) { %>
                        <div class="text-danger">
                            <%= errors.idCategory.msg %>
                        </div>
                        <% } %>
                </div>
                <div class="separador"></div>
                <div class="wrap-form tipo">
                    <label for="idType"><b>Tipo</b></label>
                    <select name="idType" id="idType" class="tipo">
                        <option value="">Elegí</option>
                        <option value="visited">Visitada</option>
                        <option value="offer" >En Oferta</option>
                    </select>
                    <% if (locals.errors && errors.idType) { %>
                        <div class="text-danger">
                            <%= errors.idType.msg %>
                        </div>
                        <% } %>
                </div>
                <div class="wrap-form-create image">
                    <label for="image"><b>La imagen del producto</b></label>
                    <input type="file" name="image" accept=".jpg, .bmp, .webp, .png " class="img-create">
                    <% if (locals.errors && errors.image) { %>
                        <div class="text-danger">
                            <%= errors.image.msg %>
                        </div>
                        <% } %>
                </div>
                <div class="wrap-form">
                    <button type="submit" class="btn-prod">Enviar</button>
                </div>
        </form>

    </main>

    <%- include("parcials/footer") %>


</body>

</html>